version: '3'
services:
  restaurant-app:
    build: ./App
    ports: 
      - "3000:3000"
    container_name: app
    restart: always
    links:
      - backend
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    container_name: 